{
  "name": "database",
  "version": "0.0.0",
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "prisma": "5.6.0"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev"
  }
}